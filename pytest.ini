[pytest]
# Auto mode lets us write `async def test_...` without decorating every test.
asyncio_mode = auto
# Make the `app` and `tests` packages importable when running from the repo root.
pythonpath = .
testpaths = tests
